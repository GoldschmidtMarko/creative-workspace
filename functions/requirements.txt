firebase_functions~=0.5.0
firebase-admin>=7.5.0
google-cloud-firestore>=2.29.0
# google-api-core 2.35.0 (released 2026-08-24) percent-encodes reserved
# characters in resource paths, mangling the literal "(default)" database
# sentinel into "%28default%29" and breaking every Firestore call with
# "400 Invalid database id %28default%29" (googleapis/google-cloud-python#18207).
# Pin below the regression until upstream ships a fix.
google-api-core<2.35.0
requests
beautifulsoup4
pandas